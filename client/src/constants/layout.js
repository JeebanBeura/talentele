import { SPACING } from "./spacing";

export const LAYOUT = {
  container: SPACING.container,
  section: `w-full ${SPACING.section.default}`,
  grid: `grid grid-cols-12 ${SPACING.grid.gap}`,
  flex: "flex items-center justify-between",
  wrapper: "w-full min-h-screen",
};
